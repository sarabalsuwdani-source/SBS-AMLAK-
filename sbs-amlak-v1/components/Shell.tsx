import Link from 'next/link';
export default function Shell({children}:{children:React.ReactNode}){const nav=[['/dashboard','لوحة التحكم'],['/properties','العقارات'],['/tenants','المستأجرين'],['/contracts','العقود'],['/payments','الدفعات'],['/reports','التقارير'],['/settings','الإعدادات']];return <div className="shell"><aside className="side"><div className="brand">SBS AMLAK</div><div className="sub">شركة اس بي اس هورايزونز لإدارة الأملاك</div><nav className="nav">{nav.map(([href,label])=><Link key={href} href={href}>{label}</Link>)}</nav></aside><main className="main">{children}</main></div>}
